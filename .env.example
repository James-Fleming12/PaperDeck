# Copy to .env for local overrides. Do NOT commit .env.
# Prefer running `paperdeck setup` which stores the key in
# ~/.config/paperdeck/config.toml (outside the repo).
OPENALEX_API_KEY=
# OPENALEX_BASE_URL=https://api.openalex.org
# PAPERDECK_DB_PATH=./data/paperdeck.db
